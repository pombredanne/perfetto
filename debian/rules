#!/usr/bin/make -f
%:
	dh $@

override_dh_auto_configure:
	tools/install-build-deps
	tools/gn gen out/release

override_dh_auto_build:
	tools/ninja -C out/release

override_dh_auto_clean:
	tools/ninja -C out/release -t clean
